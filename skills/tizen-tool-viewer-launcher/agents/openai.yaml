interface:
  display_name: "Tizen Tool Viewer Launcher"
  short_description: "Fetch TV data, validate presentation JSON, and preview"
  default_prompt: "Use $tizen-tool-viewer-launcher to fetch data, generate presentation JSON, validate it with tizen-tool-presentation-validate, save it, and launch a local TV surface preview."
