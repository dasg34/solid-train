interface:
  display_name: "TV Surface Launcher"
  short_description: "Fetch TV data, build presentation JSON, and preview"
  default_prompt: "Use $tv-surface-launcher to fetch data, generate presentation JSON, save it, and launch a local TV surface preview."
